# Spring 注册中心，配置中心配置
spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery: #注册中心
        enabled: true
        namespace: ${nacos.namespaces}
        group: ${nacos.group}
        server-addr: ${nacos.server-addr}   # 服务注册地址





